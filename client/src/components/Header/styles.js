import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignSelf: "stretch",
    padding: 24,
    height: 50,
    backgroundColor: "red",
  },
});
export default styles;
